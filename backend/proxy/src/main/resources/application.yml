server:
  port: 8080
ribbon:
  ReadTimeout: 60000
  SocketTimeout: 60000
sensitiveHeaders: Authorization, Set-Cookie
spring:
  application:
    name: proxy-service
  datasource:
    url: jdbc:mysql://15.188.232.94:3306/auth_cinema?useSSL=false&allowPublicKeyRetrieval=true
    password: olz3r_M00N
    username: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
  jackson:
    serialization:
      fail-on-empty-beans: false
  session:
    store-type: redis
  redis:
    host: 15.188.232.94
    database: 0
    password: olz3r_M00N
    timeout: 10000
    port: 6379
  cloud:
    kubernetes:
      enabled: true
      discovery:
        all-namespaces: true
dekorate:
  kubernetes:
    serviceType: "NodePort"
zuul:
  routes:
    cinema-service:
      path: /cinema-service/**
      serviceId: cinema-service
    movies-service:
      path: /movies-service/**
      serviceId: movies-service
    qrcode-service:
      path: /qrcode-service/**
      serviceId: qrcode-service