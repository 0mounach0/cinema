import { City } from '../city/city';

export class Cinema {
    id?: String;
    name?: String;
    description?: String;
    address?: String;
    longitude?: String;
    latitude?: String;
    city?: City;
}
